import React from 'react';
// import { shallow } from 'enzyme';
import App from './App';

describe('<App />', () => {
  it('should render', () => {
    // const AppComponent = shallow(<App />);
    // expect(AppComponent.find('h1').text()).toEqual('The excellent app renders');
  });
});
